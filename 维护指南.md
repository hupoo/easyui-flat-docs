# 维护指南：重新生成、推送、应对令牌过期

本仓库 `easyui-flat-docs` 已发布到公网：
> https://hupoo.github.io/easyui-flat-docs/

**部署方式：GitHub Actions（自动）**。仓库已配置工作流 `.github/workflows/pages.yml`
（基于 `actions/deploy-pages`），推送 `main` 会自动构建并发布；部署用的 `GITHUB_TOKEN`
由 GitHub 自动提供，**不依赖你的个人令牌**。

本文件说明三件事：① 如何重新生成文档；② 如何推送到 GitHub；③ 令牌 30 天过期后怎么办。

---

## 一、重新生成文档（本地）

当 jeasyui.cn 的 API 有更新，或你想调整文档内容时，按顺序跑三个脚本即可。

> 注意：本机用的是 WorkBuddy 托管的 Python venv，路径固定。请在 **Git Bash** 中执行。

```bash
# 1) 进入构建目录
cd "E:/documents/easyui文档/easyui-new-docs/build"

# 2) 用托管 venv 的 python 依次运行（首次已装好依赖，无需再装）
PY="C:/Users/ZZX/.workbuddy/binaries/python/envs/default/Scripts/python.exe"

"$PY" scrape.py     # 抓取并解析 21 个组件 API 页 -> components_raw.json（需联网）
"$PY" flatten.py    # 沿继承链扁平合并并标注「来源」 -> components_flat.json
"$PY" render.py     # 读取已编译的 tailwind.css 并内联，渲染为 ../index.html（自包含单文件）

# 3) 直接双击 ../index.html 在浏览器预览，确认无误后再推送
```

> **关于样式（Tailwind CSS）**：`render.py` 会把已编译的 `build/tailwind.css` 内联进
> `index.html`，所以日常只需上述三步 Python 命令，无需 Node。仅当你改了 `build/src.css`
> （自定义组件样式）或 `render.py` 里的工具类时，才需先用 Node 重编译：
> `npx tailwindcss -i src.css -o tailwind.css --minify`（需本地装有 `tailwindcss@3`）。
> 编译好的 `tailwind.css` 已提交仓库，离线也能正常生成页面。

三个脚本说明：
- `scrape.py`：抓取 jeasyui.cn，解析「扩展自」继承关系与属性/事件/方法三张表。
- `flatten.py`：构建继承图（含双继承），从叶子向根合并全部成员并标 `source`，输出 `components_flat.json`。
- `render.py`：把扁平数据渲染成自带搜索/导航/按来源筛选的 `index.html`。

---

## 二、推送到 GitHub

推送就是把本地的 `main` 分支同步到 GitHub。推送完成后 **GitHub Actions 会自动重新构建并发布**，
无需任何额外操作（也不用手动去 Pages 点发布）。

### 方式 A：用 Personal Access Token（当前方式）

本机目前**没有**持久保存 GitHub 凭据，每次推送需临时提供令牌（账号 `hupoo`，密码填令牌）。

```bash
GIT="/c/Program Files/Git/bin/git.exe"
cd "E:/documents/easyui文档/easyui-new-docs"

# 1) 暂存并提交
"$GIT" add -A
"$GIT" -c user.name="hupoo" -c user.email="hupoow@outlook.com" commit -m "docs: 更新文档内容"

# 2) 用令牌推送（把下方 TOKEN 换成你当前的 ghp_... 令牌）
TOKEN="ghp_你的令牌"
"$GIT" -c credential.helper= push https://hupoo:"$TOKEN"@github.com/hupoo/easyui-flat-docs.git main
```

> `-c credential.helper=` 确保令牌**不会**被写入本机凭据文件。

### 方式 B（推荐）：配置 SSH，从此免令牌、永不过期

生成一次 SSH key 并加到 GitHub，之后 `git push` 不再需要令牌：

```bash
# 1) 生成密钥（一路回车，不设置密码短语最省事）
ssh-keygen -t ed25519 -C "hupoow@outlook.com" -f "$HOME/.ssh/id_ed25519"

# 2) 把公钥内容打印出来，复制它
cat "$HOME/.ssh/id_ed25519.pub"
```

然后把复制的公钥添加到 GitHub：
**GitHub 头像 → Settings → SSH and GPG keys → New SSH key**，
Title 随便填（如 `my-pc`），Key 粘贴刚才复制的内容，Add SSH key。

最后把本地 remote 改成 SSH 地址（只需做一次）：

```bash
GIT="/c/Program Files/Git/bin/git.exe"
cd "E:/documents/easyui文档/easyui-new-docs"
"$GIT" remote set-url origin git@github.com:hupoo/easyui-flat-docs.git
```

之后日常推送只需：`"$GIT" push`（或用方式 A 提交后 `git push`）。

---

## 三、令牌 30 天过期后怎么办

**重要：令牌过期不会让线上站点下线。** 已发布的 GitHub Pages 内容是持久保存的，
过期只影响「用这个令牌做新的推送」。

你有三种应对：

1. **重新生成一个令牌**（最省事）
   按下方「如何获取个人访问令牌」重新生成，**只需 `repo` 作用域即可**（工作流文件已在仓库里，
   日常更新不会再改动它，因此不需要 `workflow` 作用域）。拿到新的 `ghp_...` 后按方式 A 推送即可。
   旧令牌可在 GitHub **Settings → Developer settings → Personal access tokens** 里手动 Revoke。

2. **改用 SSH（一劳永逸）**：见方式 B，配置后永不过期。

3. **什么都不做**：只要不更新文档，站点会一直在线，无需任何操作。

> 部署本身由 GitHub Actions 用 `GITHUB_TOKEN` 自动完成，**与个人令牌是否过期无关**——
> 只要把新代码推上去，Actions 就会自动重新发布。

---

## 四、如何获取个人访问令牌（备查）

> 现状：仓库已切到 GitHub Actions 部署，工作流文件 `.github/workflows/pages.yml` 已就位。
> 因此**日常更新文档只需 `repo` 作用域的令牌**（推送代码即可，部署自动进行）。
> 仅当你要**改动 / 重推工作流文件**时，才需要额外勾选 `workflow` 作用域。

步骤：

1. 登录 https://github.com
2. 右上角头像 → **Settings**
3. 左侧最底部 → **Developer settings**
4. **Personal access tokens** → **Tokens (classic)**
5. 点 **Generate new token (classic)**
6. Note 随便写，如 `easyui-docs-push`
7. Expiration：可选 30 / 60 / 90 天，或 No expiration（不建议长期）
8. **Scopes 勾选**（关键）：
   - ☑ `repo`（展开后默认全选，含读写代码权限——**日常推送必选**）
   - ◻ `workflow`（**仅当要修改工作流文件时**才需要；日常文档更新不用勾）
9. 点 **Generate token**
10. 复制那串 `ghp_...` 发给助手

**附：workflow 作用域曾是「切到 Actions 部署」的门槛**
早期 PAT 只有 `repo` 时，推送 `.github/workflows/*.yml` 会被 GitHub 拒绝（安全限制：
推送工作流文件必须显式拥有 `workflow` 作用域）。当时因此先走「从分支部署」上线，
后来用带 `workflow` 的令牌把工作流推上去并切到 Actions。现在这一步已完成，后续无需再纠结。
